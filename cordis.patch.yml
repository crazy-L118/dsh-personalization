# dsh-personalization bundle patch: inserts the personalization plugin row
# into the profile roster. Applied as a profile bundle layer (the
# `dsh.bundle.patch` manifest field).
- insert:
    - id: personalization
      name: 'dsh-personalization'
